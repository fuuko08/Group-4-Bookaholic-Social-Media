
<div class="container mt-4 mb-5">
  <div class="d-flex justify-content-center row">
    <div class="col-md-8">
      <!-- Create a post button -->
      <div class="feed p-2">
        <button type="button" id="createBtn" class="btn btn-primary btn-sm" data-toggle="modal" data-target="#exampleModalLong">
          Create a Post
        </button>
        <div>
        {{>postModal}}          
        </div>
        <div>
        {{>allPosts}}          
        </div>
