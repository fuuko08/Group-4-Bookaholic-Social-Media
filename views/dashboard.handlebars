{{#if posts.length}}
<section>
    <h2>Look at all the books you have read</h2>
    {{#each posts as |post|}}
        {{> dashboard-info post}}
    {{/each}}

</section>
{{/if}}

<button type="submit" class="btn"><a class="link" href="/dashboard/new">Add New Post</a></button>

<script src="/js/newpost.js"></script>