{{!-- <h2 class="card-title">Look at all the books you have read</h2>
<button type="submit" class="btn"><a class="link" href="/dashboard/new">Add New Post</a></button>


{{#if posts.length}}
<section>
    {{#each posts as |post|}}
        {{> dashboard-info post}}
        {{> imageCard post}}
    {{/each}}

</section>
{{/if}} --}}
